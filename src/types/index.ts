export interface DataType {
  id: number;
  title: string;
  image: string;
  destinationName: "Home" | "Skeleton";
}

export interface CarouselDataType {
  title: string;
  location: string;
  date: string;
  poster: string;
}
